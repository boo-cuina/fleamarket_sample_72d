.account-container
  .account-container__header
    %img{alt: "rogo", src: "http://furima.tokyo/assets/logo-d3d78326971d78b06e3d6f0ba666d025a8ad681286b4d9e00e7dbe8673bcfd23.svg"}
  .account-container__main
    .account-container__main-form
      %h2.account-main__head
        会員情報入力
      .account-main__content
        .account-main__content-form
        -# = form_with @user, local: true do |f|
          .form-group
            -# = f.label ニックネーム
            %span.form-require
              必須
            .form-input
              = f.text_field :nick_name, placeholder: "例）メルカリ太郎", class: "input-default"
          .form-group
            -# = f.label メールアドレス
            %span.form-require
              必須
            .form-input
              = f.text_field :email, placeholder: "PC・携帯どちらでも可", class: "input-default"
          .form-group
            -# = f.label パスワード
            %span.form-require
              必須
            .form-input
              -# = f.text_field :password, placeholder: "7文字以上の半角英数字", class: "input-default"
              %p ※英字と数字の両方を含めて設定してください
          %h3.account-identification
            本人確認
          %p.text-identification
            安心・安全にご利用いただくために、お客様の本人情報の登録にご協力ください。他のお客様に公開されることはありません。
          .form-group
            -# = f.label "お名前(全角)"
            %span.form-require
              必須
            .form-input
              = f.text_field :surname, placeholder: "例）山田", class: "input-default"
              = f.text_field :name, placeholder: "例）太郎", class: "input-default"
          .form-group
            -# = f.label "お名前カナ(全角)"
            %span.form-require
              必須
            .form-input
              = f.text_field :surname_kana, placeholder: "例）ヤマダ", class: "input-default"
              = f.text_field :name_kana, placeholder: "例）タロウ", class: "input-default"
          .form-group
            -# = f.label "生年月日"
            %span.form-require
              必須
            .birthday-form
              .birthday-select
                .select-wrap
                  -# = f.select :birth_year
                %i.fas.fa-angle-down.fa-lg
              %span
                年
              .birthday-select
                .select-wrap
                  -# = f.select :birth_month
                %i.fas.fa-angle-down.fa-lg
              %span
                月
              .birthday-select
                .select-wrap
                  -# = f.select :birth_day
                %i.fas.fa-angle-down.fa-lg
              %span
                日
            %p.personal-information-text
              ※本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .actions
            = f.submit "Log in", class: "btn"